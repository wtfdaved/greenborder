User-agent: *
Allow: /

# Not pages: build inputs, source data and scaffolding.
Disallow: /.git/
Disallow: /.claude/
Disallow: /csv/
Disallow: /public/
Disallow: /tools/

# The old standalone directory is a redirect stub. It is noindex in the
# page itself; leaving it crawlable is what lets Google see that and
# fold its history into the home page.
Allow: /directory.html

# Crawl-delay is ignored by Google and honored by the bots we care
# least about, so it is set only where it does something.
User-agent: MJ12bot
Crawl-delay: 10

User-agent: BadBot
Disallow: /

Sitemap: https://greenborder.org/sitemap.xml
